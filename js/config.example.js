// Firebase web config template.
// Copy this file to `js/config.js` and fill in your project's values.
// `js/config.js` is gitignored.
//
// Note: Firebase web SDK config (apiKey etc.) is not considered a secret.
// Security is enforced via Authentication + Security Rules.
// Separating it here is for environment switching and hygiene, not secrecy.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "0000000000",
  appId: "1:0000000000:web:xxxxxxxxxxxxxxxx",
};
